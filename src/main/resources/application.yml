spring:
  data:
    redis:
      host: localhost
      port: 6379
      connect-timeout: 2000ms
  application:
    name: PetProjectWeather
  datasource:
    url: jdbc:postgresql://localhost:5432/petWeather
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
clickhouse:
  jdbc:
    url: jdbc:clickhouse://localhost:8123/pet_weather
    user: pet_user
    password: qwerty

